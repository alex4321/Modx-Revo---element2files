Modx-Revo---element2files
=========================

Plugin for loading modx revolution elements from files.

Need "system setting" elements_path (e.g. elements_path="assets/element") where can be located directories "templates", "snippets", "chunks", "plugins". Each directory must have "elements" php with element name/file and other table fields. You have example in repository.
